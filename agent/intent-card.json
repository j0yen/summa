{
  "schema": "autobuilder.intent_card.v1",
  "prd_source": "/home/jsy/Documents/PRDs/build-queue/PRD-summa-gate-debt.md",
  "intent_slug": "summa-gate-debt",
  "root_motivation": "Three summa PRDs have finished code and green tests but cannot tag or archive: the gate reports ~10 blocking receipts that are repo debt, not PRD defects. Verified on the redbaron checkout: no `scripts/` dir (run-metrics.sh and audit.sh missing, which kills intake/proof-receipt/session-trace and risk-gate), no `.github/workflows/` (ci-checks counts 0 runs), `rust-version = \"1.85\"` while locked deps need 1.88 (msrv-verify and flake-audit red), no fixtures lane in `agent/proof-lanes.toml` (vti-plan flags non-.rs fixture files), no ac-traceability source in the repo, cargo-deny findings (lopdf advisory via pdf-extract, a rejected license), and tags stopped at v0.4.0 so receipt base_refs mis-attribute sibling diffs. Fix them once, at the repo level.",
  "user_persona": "UNSPECIFIED \u2014 no existing intent card to carry forward from; set by hand (intent-card-refresh.sh cannot source this from a PRD).",
  "unfakeable_metric": {
    "name": "acceptance_tests_passing_count",
    "lower_is_better": false,
    "harness_command": "scripts/run-metrics.sh",
    "target": null
  },
  "acceptance_criteria": [
    {
      "id": "AC1",
      "level": "MUST",
      "description": "Given the extended repo, When `autobuilder loop --iteration 0 --trace` runs, Then it no longer aborts on missing `scripts/run-metrics.sh` and the intake, proof-receipt, and session-trace receipts are produced.",
      "test": "tests/acceptance_ac1.rs"
    },
    {
      "id": "AC2",
      "level": "MUST",
      "description": "Given `scripts/audit.sh` exists, When the gate's risk-gate producer runs, Then the risk-gate receipt is produced instead of reported missing.",
      "test": "tests/acceptance_ac2.rs"
    },
    {
      "id": "AC3",
      "level": "MUST",
      "description": "Given `rust-version = \"1.88\"`, When msrv-verify runs its toolchain check three times, Then all runs exit 0 and the flake-audit receipt stops flagging the MSRV check.",
      "test": "tests/acceptance_ac3.rs"
    },
    {
      "id": "AC4",
      "level": "MUST",
      "description": "Given the fixtures lane in proof-lanes.toml, When `autobuilder vti-plan --project .` runs at HEAD, Then `tests/fixtures/vault/**` paths route with nonzero confidence and vti-plan reports unrouted=0.",
      "test": "tests/acceptance_ac4.rs"
    },
    {
      "id": "AC5",
      "level": "MUST",
      "description": "Given the ac-traceability source is in place, When that receipt producer runs, Then it stops reporting \"no PRD-*.md found in project root\".",
      "test": "tests/acceptance_ac5.rs"
    },
    {
      "id": "AC6",
      "level": "MUST",
      "description": "Given the dependency and deny.toml changes, When `cargo deny check advisories licenses` runs, Then the lopdf advisory and the rejected-license finding are both resolved, and `cargo test` stays fully green.",
      "test": "tests/acceptance_ac6.rs"
    },
    {
      "id": "AC7",
      "level": "MUST",
      "description": "Given the backfilled tags, When `git describe --tags` runs at the v0.6.0 commit and at HEAD, Then base_ref resolution yields v0.6.0 (not v0.4.0), and both tags are annotated and pushed.",
      "test": "tests/acceptance_ac7.rs"
    },
    {
      "id": "AC8",
      "level": "MUST",
      "description": "Given all P0 fixes landed, When `extend-gate.sh /home/jsy/repos/summa --head <landed sha>` runs, Then the blocking set contains none of: intake, proof-receipt, session-trace, risk-gate, msrv-verify, flake-audit, vti-plan, ac-traceability (ci-checks and reviewer-agent may still block pending CI history).",
      "test": "tests/acceptance_ac8.rs"
    },
    {
      "id": "AC9",
      "level": "SHOULD",
      "description": "Given the pushed workflow, When one CI run completes on GitHub, Then a subsequent gate run's ci-checks receipt reports run_count >= 1.",
      "test": "tests/acceptance_ac9.rs"
    },
    {
      "id": "AC10",
      "level": "MUST",
      "description": "Given the finished diff, When version and tests are checked, Then Cargo.toml is 0.6.1, `cargo test` is fully green, and clippy is clean on the diff.",
      "test": "tests/acceptance_ac10.rs"
    }
  ],
  "scope": [],
  "non_goals": [],
  "hard_constraints": {
    "rust_edition": "2024",
    "target_kind": "cli",
    "deny_unsafe": true
  },
  "five_whys_trace": [
    {
      "why": 1,
      "q": "Card auto-generated by intent-card-refresh.sh \u2014 was a 5-Whys interview conducted for it?",
      "a": "No. This card was mechanically regenerated from the landed PRD's frontmatter and body by scripts/intent-card-refresh.sh; no five-whys interview ran. See prd_source for the human-authored PRD (/home/jsy/Documents/PRDs/build-queue/PRD-summa-relevant-recall.md) this ships from."
    }
  ],
  "created_at": "2026-09-06T00:00:00Z"
}
